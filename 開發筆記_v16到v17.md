# v16 → v17 修正紀錄（2026/08/11）

同事回報：公文若需到附件下載區抓外部附件，一律失敗，畫面出現
`❌ 支線錯誤: not enough values to unpack (expected 2, got 1)`。
排查後發現不只一個問題，以下按修正順序記錄。

---

## 1. 支線下載視窗開不起來（根因，v16 新引入）

**症狀**：`not enough values to unpack (expected 2, got 1)`

**原因**：`download_external_files_gui` 裡

```python
co.auto_port()                          # ① _address = ''，等後面自動配 port
co.set_user_data_path(PROFILE_DIR_EXT)  # ② _auto_port = False，但沒補回 _address
```

DrissionPage 4.1.1.4 的兩個方法互相打架：

- `chromium_options.py:379` `auto_port()` → `self._address = ''`
- `chromium_options.py:364` `set_user_data_path()` → `self._auto_port = False`

結果 `_address=''` 且 `_auto_port=False`，`chromium.py:467` 的自動配 port 整段跳過，
最後 `browser.py:30` 的 `ip, port = address.split(':')` 拿到 `['']` 就爆了。

v15 只有 `auto_port()` 所以正常；v16 為了「支線專用乾淨設定檔」加上第②行才壞掉。

**修法**：換順序，改用固定 port。

```python
co.set_local_port(_free_port(9333))     # 先定 port（寫好 _address 並關掉 auto_port）
co.set_user_data_path(PROFILE_DIR_EXT)  # 再設 user data（只動 _auto_port，不碰 _address）
```

---

## 2. 附件上傳視窗「存在」不等於「開啟」

**症狀**：附件鈕看起來被按下去，但沒跳視窗，程式空轉一陣後回報「已按存檔」。

**原因**：SPEED 的附件視窗是**同頁面的 modal**，頁面載入時就已存在於 DOM、只是隱藏。
v16 用 `speed.ele('#selectfiles')` 判斷「介面開好了」，視窗沒開也會判定成功
→ 檔案被 `input()` 塞進隱藏欄位，什麼都沒發生。

**修法**：改判斷**可見性**（`states.is_displayed`），用視窗內的
「附件類別 / 選擇檔案 / 發文附件大小總計」當指紋。點擊改為三招重試，每招點完驗證視窗是否真的出現。

---

## 3. 存檔鈕誤按到工具列的「公文存檔」

**症狀**：跳出「存檔成功！」，但附件其實沒上傳。

**原因**：存檔鈕的 fallback 鏈

```python
('...AttachmentSave"]', '@data-speed-action=AttachmentSave',
 'text:存檔',      # ← 掉到這裡，抓到工具列那顆公文存檔
 'text:確定', 'text:儲存')
```

附件視窗底部的鈕實際叫「**確定**」，但 `text:存檔` 排在前面先中。

**修法**：移除 `text:存檔`，只認 `AttachmentSave` 或視窗內**可見的**「確定」。
找不到就停手並保留視窗，寧可不存也不誤按公文存檔。

---

## 4. 本文下載「先刪舊檔再猜檔名」

**症狀**：整個下載資料夾變空，log 只說找不到本文 PDF。

**原因**：v16 先 `os.remove(舊檔)`，再點下載、等 15 秒，然後用檔名 `startswith` 去猜。
下載慢一點或 Chrome 改了名就找不到，而舊檔已經先刪了。

**修法**：點擊前記錄資料夾內容，等「新增且大小穩定」的檔案出現（最長 40 秒），落地後自己改名。
不預先刪檔、不猜檔名。

---

## 5. 外部附件用 `max(getmtime)` 抓錯檔

**原因**：

```python
for _ in range(3):          # 同一附件點 3 次
    link.click(by_js=True)
    while ...:
        ...
        if ...: pass        # 空 pass，判斷結果被丟掉
        break               # while 第一圈就跳出，等待邏輯完全失效
latest_file = max(整個資料夾, key=getmtime)   # 挑「最新檔」改名
os.rename(latest_file, target)
```

沒過濾 `.crdownload`、沒確認是這次下載的 → **公文本文 PDF 會被改名成附件名**。

**修法**：同 #4，只認這次點擊後新增的檔案，且只點一次。

---

## 6. 靜默失敗

`check_pdf_for_external_content` 在下列情況一個字都不印，直接當作「沒有外部附件」：

- 沒裝 pdfplumber（`if not PDFPLUMBER_OK: return`）
- PDF 抽不到文字
- 找不到剛下載的本文

**修法**：每一步都輸出診斷，包含實際使用的 Python 直譯器路徑
（一台電腦多個 Python 是最容易誤判的坑）。

---

## 7. 上傳用固定 sleep 賭時間

v16 每個檔案之間 `time.sleep(2)`。附件大一點就可能被 plupload 吃掉。

**修法**：改成上傳後**等該檔名出現在附件清單表格**才傳下一個。
小檔不空等、大檔不漏掉，漏了會誠實回報而非假裝成功。

---

## 8. `pin_fill` 的防護是死碼

```python
def pin_fill(...):
    ...
    return True          # 不論如何都 True

# 呼叫端
if not pin_fill(...):    # 永遠是 False
    raise RuntimeError("登入PIN輸入驗證失敗")   # 永遠不會執行
```

自然人憑證 PIN 連續錯誤會鎖卡。歸檔那邊有讀回核對的煞車，登入這段沒有。

**修法**：只在「讀得到值、而且確定不符」時回傳 False。
讀不到值仍放行（部分欄位 JS 讀不到真值，硬驗會誤殺）。

---

## 9. 內外部附件編號會撞

兩者都從 2 開始編，同一份公文並存時會交錯：
`-2-Ext-`、`-3-附件`、`-4-Ext-`。

**修法**：外部附件改走獨立序列 `E1, E2, E3…`，排序時自然聚在內部附件之後。

---

## 其他改動

- 新增 `collect_doc_nobrs()`：收文清單的公文列蒐集集中管理。
  `DOC_ROW_TAGS` 預設 `('承辦',)`；測試時可加入「退」等屬性
  （非承辦的列會額外要求該列有純數字文號連結，以排除表頭/選單）。
- 外部附件關鍵字擴充：加入 `附件下載區`、`附件下載`、`doc2-attach`
  （實測公文只靠「驗證碼」一個關鍵字命中，太脆弱）。
- 文號 regex 改為優先錨定「發文字號」
  （說明段常引用別的字號，例如「臺教國署國字第…號」，原本的
  `字第(\d+)號` 靠「第一個出現的剛好是對的」僥倖過關）。
- 調速參數集中到檔案上方「⏱ 調速區」。
- `.bat` 一律純 ASCII + CRLF：cmd 會用 Big5 解讀批次檔內容，
  檔名寫中文會變亂碼，改用 `for %%f in ("%~dp0*v17.py")` 讓 cmd 自己比對。

## 待辦

- [ ] `DOC_ROW_TAGS` 若要正式支援「退」，需確認該狀態下附件鈕是否可用
- [ ] 上傳檔名 40 字截斷是猜測值，系統真正上限未驗證
      （`上傳測試檔\檢核表.md` 的結果欄仍空白）
- [ ] 本文檔名 `max_len=200` 偏長，加上路徑接近 Windows 260 字元上限
- [ ] 歸檔分類號目前全部 1199，未依公文性質分類
