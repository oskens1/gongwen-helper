# 公文小助手 v15 — 讀我

新北市公務雲二代公文的自動化小工具：**自動收文（下載）＋自動歸檔＋附件回填上傳**。

## 一、系統需求
- Windows，且已安裝 **Google Chrome**。
- 已安裝並設定好 **自然人憑證元件（HiCOS）**，插著憑證卡（登入公務雲用）。
- 兩種執行方式擇一：
  1. **有裝 Python**：需安裝套件 `DrissionPage`、`pdfplumber`、`pillow`
     （命令列執行 `pip install DrissionPage pdfplumber pillow`）。
  2. **沒裝 Python**：請索取打包好的 `公文小助手` 資料夾，雙擊裡面的 `公文小助手.exe`。

## 二、怎麼執行
- Python 版：雙擊 **`啟動_公文小助手.bat`**。
- 打包版：雙擊 `dist\公文小助手\公文小助手.exe`。

## 三、三大功能
1. **自動收文（開始下載）**：登入 → 進二代公文 → 待辦理區，逐筆下載公文 PDF 到你選的資料夾。
   若公文內含「需驗證碼到別的網頁抓的外部附件」，會自動抓下來並**回填上傳**回該公文。
2. **自動歸檔（簽收＋存查）**：自動簽收所有「已決」公文 → 逐筆核判存查歸檔（分類號固定 **1199**）。

## 四、操作步驟
1. 開啟後，輸入 **PIN 碼**（憑證卡密碼）。
2. 選「**儲存位置**」（收文下載要存的資料夾）。
3. 按「🚀 自動收文」或「📂 自動歸檔」。
4. 過程看黑底 Log；完成會跳提示。

## 五、注意事項
- **「無頭模式」請勿勾選**：背景模式讀不到自然人憑證，會登入失敗。
- 歸檔分類號目前一律 **1199（其他）**。
- **PIN 一天只需輸入一次**：當天第一次歸檔會要 PIN，之後自動略過。
- 附件回填：系統規定**附件描述 ≤ 50 字**，程式已自動把過長檔名截短再上傳。
- 執行時若 Chrome 跳「要更新密碼嗎」→ 按「不用了，謝謝」（別讓瀏覽器存 PIN）。

## 六、疑難排解
- **exe 被防毒擋**：這是 PyInstaller 常見誤判。請在防毒把該資料夾加入信任，
  或改用 `打包EXE.bat`（已用 onedir＋關 UPX，誤判率低很多）。
- **開網頁失敗 / WebSocket 404**：把所有 Chrome 視窗全部關掉（含背景），再重跑。
- **找不到 pdfplumber**：只影響「外部附件偵測」；`pip install pdfplumber` 即可，
  不裝也能開程式、歸檔正常。

## 七、版本
- v15（2026/07/04）：附件回填上傳改用實測驗證的穩定流程（只認新開視窗、附件鈕重試、
  上傳欄備援、多種存檔鈕、描述過長自動截短）；收文改智慧等待、二代公文用 SSO 連結。
