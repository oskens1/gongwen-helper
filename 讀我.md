# 公文小助手 v17

新北市公務雲二代公文的自動化小工具：**自動收文（下載）＋外部附件抓取回填＋自動歸檔**。

---

## 一、系統需求

- Windows，已安裝 **Google Chrome**
- 已安裝 **自然人憑證元件（HiCOS）**，並插著憑證卡
- **Python 3.10 以上**（已在 3.14 實測跑通完整流程）
  > 打包成 exe 時若 PyInstaller 不支援手上的 Python 版本，改用 3.12 打包即可，
  > 執行端不受影響（exe 會自帶直譯器）。

## 二、安裝

1. 雙擊 **`安裝套件.bat`** — 它會把套件裝到「等下真正會用到的那顆 Python」。

   > ⚠️ 一台電腦常同時裝了 Anaconda 和 python.org 版。
   > 在 Anaconda Prompt 裡 `pip install` 不一定有用 —— 要看是不是同一顆直譯器。
   > 這支 bat 會印出完整路徑，照它裝就不會錯。

2. 雙擊 **`啟動_公文小助手v17.bat`** 執行。它啟動前會自我檢查套件。

沒裝 Python 的同事：請索取打包好的資料夾（見第六節），**整個資料夾**一起用。

## 三、三大功能

1. **自動收文** — 登入 → 二代公文 → 待辦理區，逐筆下載公文 PDF 到指定資料夾
2. **外部附件** — 若本文提到「請至附件下載區…驗證碼：XXX」，自動去附件下載區抓檔，**再回填上傳**到該公文
3. **自動歸檔** — 自動簽收「已決」公文 → 逐筆核判存查（分類號可自訂，預設 1199）

## 四、操作

1. 輸入 **PIN 碼**（憑證卡密碼）
2. 選 **儲存位置**
3. 需要的話改 **歸檔分類號**（4 碼數字）
4. 按「🚀 自動收文」或「📂 自動歸檔」
5. 看黑底 Log；右上角 ⚙ 有說明

## 五、注意事項

- **「無頭模式」請勿勾選** — 背景模式讀不到自然人憑證，會登入失敗
- **執行前把所有 Chrome 關掉**（含背景），否則新視窗會去接舊的瀏覽器
- **PIN 一天只需輸入一次** — 當天第一次歸檔會要，之後自動略過
- 附件檔名**不得含空白**（系統規定），程式會自動清除
- Chrome 若跳「要更新密碼嗎」→ 按「不用了，謝謝」

## 六、打包成 exe

雙擊 **`打包EXE.bat`**（onedir + 關 UPX，降低防毒誤判）。
成品在 `dist\公文小助手v17\`，**整個資料夾**一起交付，只複製 exe 會打不開。

## 七、檔名規則

| 類型 | 格式 | 範例 |
|---|---|---|
| 本文 | `MMDD-第幾筆-1-主旨` | `0811-1-1-函轉教育部….PDF` |
| 內部附件 | `MMDD-第幾筆-N-附件` | `0811-1-2-附件.pdf` |
| 外部附件 | `MMDD-第幾筆-EN-Ext-名稱` | `0811-1-E1-Ext-附件1.pdf` |

外部附件走獨立的 `E` 序列，避免與內部附件撞號。

## 八、調速

RPA 跑太快導致動作被吃掉時，改這幾個數字（都在 .py 檔案上方，有標「⏱ 調速區」）：

| 參數 | 預設 | 作用 |
|---|---|---|
| `PACE_SEC` | 2 | 全域步伐，影響所有動作 |
| `ATTACH_OPEN_WAIT` | 3 | 點「附件」前先等幾秒 |
| `UPLOAD_ROW_TIMEOUT` | 20 | 每個附件等它出現在清單的上限 |
| `UPLOAD_GAP` | 1.5 | 兩個附件之間的間隔 |
| `SAVE_BEFORE_WAIT` / `SAVE_AFTER_WAIT` | 2 / 3 | 按「確定」前後的等待 |

另有 `DOC_ROW_TAGS`（預設 `('承辦',)`）——測試時想納入「退」等其他屬性的公文，改成 `('承辦', '退')`。

## 九、疑難排解

| 症狀 | 原因 / 解法 |
|---|---|
| `not enough values to unpack (expected 2, got 1)` | v16 的埠號 bug，v17 已修 |
| `⚠️ 未安裝 pdfplumber` | 執行 `安裝套件.bat`（注意要裝對直譯器） |
| `X 等不到檔案落地` | Chrome 沒下載成功。關掉所有 Chrome 重跑 |
| `X 開不了附件上傳視窗` | 調大 `ATTACH_OPEN_WAIT` |
| `⚠️ 回填部分完成` | 調大 `UPLOAD_ROW_TIMEOUT` / `UPLOAD_GAP` |
| 開網頁失敗 / WebSocket 404 | 關掉所有 Chrome（含背景）再重跑 |
| exe 被防毒擋 | PyInstaller 常見誤判，把整個資料夾加入信任 |

## 十、⚠️ 上傳到 GitHub 前

`settings.json` 含**明文 PIN**，已列入 `.gitignore`。
新增檔案時請確認沒有把它或 `dist/` 一起 commit 進去。
